import { SafeAreaView, Text } from "react-native-safe-area-context";

const transferMoney = () => {
  return (
    <SafeAreaView>
      <Text>hello</Text>
    </SafeAreaView>
  );
};

export default transferMoney;
